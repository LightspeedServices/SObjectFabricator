name: Static Code Security Analysis (apex)

on:
  push:
    branches: [ master, main, develop ]
  pull_request:
    branches: [ master, main, develop ]

jobs:
  analyze:
    permissions:
      actions: read
      contents: read
      security-events: write
    uses: lightspeed-security/code-scanning-workflows/.github/workflows/apex.yml@main
